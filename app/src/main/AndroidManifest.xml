<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.team">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".haihua.TwoPkActivity"></activity>
        <activity android:name=".haihua.OnePkActivity" />
        <activity android:name=".haihua.WlActivity" />
        <activity android:name=".haihua.YanZhengActivity" />
        <activity android:name=".haihua.JobCardActivity" />
        <activity android:name=".haihua.MessageActivity" />
        <activity
            android:name=".haihua.HuaActivity"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:screenOrientation="portrait" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>